"""
Bonus objective — Rebrickable API client.

Owns: a thin wrapper around the Rebrickable API (auth'd with
app/params.REBRICKABLE_API_KEY), in particular the get_set_match endpoint —
given our inventory (part -> count) from app/pipeline/inference.py, return
which sets we could build and what's missing from each.

Requires a free API key from https://rebrickable.com/api/ in .env.
Not implemented yet — see README.md, Bonus objective.
"""
