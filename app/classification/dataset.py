"""
Objective 1, Step 1 — dataset prep.

Owns: picking the 50 classes to keep from data/lego-dataset-classification,
copying/symlinking them into a working subset, and the tf.keras
data pipeline (tf.keras.utils.image_dataset_from_directory or tf.data) that
Step 2 (train.py) trains on (with a real+render split and a train/val/test
split).

Not implemented yet — see README.md Phase 1, Step 1.
"""
