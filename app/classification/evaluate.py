"""
Objective 1, Step 3 — evaluation.

Owns: loading the saved Keras model (app/config.CLASSIFICATION_MODEL_PATH)
and scoring top-1 accuracy on the held-out test split (model.evaluate),
reporting whether we've crossed app/config.CLASSIFICATION_ACCURACY_TARGET
(70%). This is the gate before we move on to Objective 2.

Not implemented yet — see README.md Phase 1, Step 3.
"""
