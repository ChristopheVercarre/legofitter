# Slim per-container pins. Keep versions in sync with requirements.txt
# (and requirements-cloud.txt) -- same versions everywhere, smaller set here.
fastapi==0.115.0
uvicorn==0.30.6
# HEIC uploads from iPhones -- needed once the predict endpoint decodes images
pillow-heif==0.22.0
python-multipart==0.0.20
httpx==0.27.2
